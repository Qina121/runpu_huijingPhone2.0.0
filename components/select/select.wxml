<!--components/select/select.wxml-->
<view class='com-selectBox'>
    <view class='com-sContent' bindtap='selectToggle'>
        <view class='com-sTxt'>{{propNowText}}</view>
        <!-- <image src='../../image/乡村.png'  class='com-sImg'  animation="{{animationData}}"></image> -->
        <!-- <image src='../../image/乡村.png'  class='com-sImg'  animation="{{animationData}}"></image> -->
    </view>
    <view class='com-sList' wx:if="{{selectShow}}">
        <view wx:for="{{propArray}}" data-index="{{index}}" wx:key='' class='com-sItem' bindtap='setText'>{{item.text}}</view>
    </view>
</view>
